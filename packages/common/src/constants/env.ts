export enum AppEnvironments {
  production = "production",
  staging = "staging",
  development = "development",
  local = "local",
}

export enum NodeEnvs {
  production = "production",
  development = "development",
  test = "test",
}
