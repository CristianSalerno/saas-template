export {
  Lifecycle,
  type InjectionToken,
  type DependencyContainer,
  inject as Inject,
  scoped as Scoped,
  container as Container,
  registry as Registry,
  injectable as Resolver,
  injectable as Service,
  injectable as Repository,
  singleton as Global,
  delay,
} from "tsyringe";
